image: golang:latest

before_script:
  - apt update && apt -y install make

stages:
- test

sast:
  stage: test
include:
- template: Security/SAST.gitlab-ci.yml

unit_tests:
  stage: test
  script:
    - make test
